import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    mapContainer: {
        width: '90%',
        height: '75%'
    },
    loadingText: {
        textAlign: 'center',
        fontSize: 20,
        color: '#f4a261'
    }
});

export default styles;